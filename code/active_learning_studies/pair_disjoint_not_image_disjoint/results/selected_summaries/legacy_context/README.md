# Legacy sequential exploration

This early exploratory setting used 70 initial labeled pairs, a 100-pair candidate pool, two-pair acquisition batches, and a 30-image ideal test set. Its split, schedule, and evaluation differ from fixed single-shot studies, so it is preserved for context and not pooled into the current conclusion.

